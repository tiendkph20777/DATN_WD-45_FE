export interface IProducts {
    id: string | number;
    name: string,
    price: number,
    des: string
}
export interface IComment {
    id: string | number;
    name: string,
    price: number,
    des: string
}